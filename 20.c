/** Preview of comming attractions! */
#include <stdio.h>

int main(int argc, char *argv[]) {
  int *ptr = malloc(200);
  // Dynamic allocation -- What's it really good for?
  free(ptr);
  return 0;
}
